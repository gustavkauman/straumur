{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [ "CLOUDFLARE_ENV" ],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
